name: Auto Assign

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - ready_for_review

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Auto assign PR
        uses: kentaro-m/auto-assign-action@v2.0.2
